# Copia este archivo a ".env" y completa los valores reales.
# NUNCA subas el archivo ".env" real al repositorio (ya está en .gitignore).

# --- Base de datos ---
POSTGRES_DB=medalert
POSTGRES_USER=medalert
POSTGRES_PASSWORD=P0s31d0n

# --- RabbitMQ ---
RABBITMQ_DEFAULT_USER=medalert
RABBITMQ_DEFAULT_PASS=H3rm3s

# --- HAPI FHIR (mock server que simula RAS) ---
FHIR_SERVER_URL=http://localhost:8080/fhir

# --- Twilio (SMS / WhatsApp) — usar credenciales de cuenta sandbox mientras se desarrolla ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_SMS_FROM_NUMBER=
TWILIO_WHATSAPP_FROM_NUMBER=

# --- SendGrid (email) ---
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=notificaciones@medalertpro.test

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379
